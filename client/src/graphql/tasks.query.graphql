query GetTasks {
  tasks {
    id
    title
    subtitle
    notes
    completed
    priority
    userId
    parentId
    children {
      id
      title
      subtitle
      notes
      completed
      priority
      userId
    }
  }
}
